# 📋 Как одобрять истории (Approve Stories)

## 🔍 Где находится функция одобрения?

Файл: **`app/admin/stories/page.tsx`**

Строка: **281**

## 📍 Как найти в интерфейсе:

1. Откройте ваш сайт в браузере
2. Перейдите по адресу: `http://localhost:3000/admin/stories`
3. Вы увидите список историй со статусом `PENDING`
4. Под каждой историей есть две кнопки:
   - **✅ Approve (Одобрить)** - зеленая кнопка
   - **❌ Reject (Отклонить)** - красная кнопка

## 🎯 Процесс одобрения:

1. Просмотрите историю в админ-панели
2. При необходимости отредактируйте текст в полях `summary` и `sections`
3. Нажмите кнопку **"✅ Approve (Одобрить)"**
4. История автоматически изменит статус на `APPROVED`
5. После одобрения история станет видимой на публичных страницах:
   - `/section` - список всех одобренных историй
   - `/section/[id]` - детальная страница истории
   - `/dashboard` - дашборд со историями

## 💻 В коде:

```typescript
// app/admin/stories/page.tsx, строка 281
<button
  onClick={() => updateStatus(story, 'APPROVED')}
  className="inline-flex items-center rounded-md bg-emerald-600 px-4 py-2 text-sm font-medium text-white hover:bg-emerald-700 shadow-md hover:shadow-lg transition-all"
>
  ✅ Approve (Одобрить)
</button>
```

## 🔐 Требования:

- Вы должны быть авторизованы через Clerk
- Только авторизованные пользователи могут видеть админ-панель и одобрять истории

## 📝 Что происходит при одобрении:

1. Статус истории меняется с `PENDING` на `APPROVED`
2. Изменения сохраняются в файл `app/data/stories.json`
3. История становится доступной через API: `/api/stories?status=APPROVED`
4. История отображается на всех публичных страницах сайта

