# 🚀 Настройка Vercel для форкнутого проекта

## Проблема: Vercel не обновляется после форка

Если вы сделали форк проекта, Vercel может быть подключен к оригинальному репозиторию, а не к вашему форку.

## Решение:

### Вариант 1: Подключить ваш форк к Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите в аккаунт
3. Перейдите в **Dashboard** → **Add New...** → **Project**
4. Выберите ваш форк репозитория (`amiroesh1/myfolio`)
5. Настройте проект:
   - **Framework Preset**: Next.js
   - **Root Directory**: `./` (по умолчанию)
   - **Environment Variables**: добавьте все переменные из `.env.local`

### Вариант 2: Отключить старый проект и создать новый

1. Если у вас уже подключен проект:
   - Зайдите в **Settings** → **Git**
   - **Disconnect** старый репозиторий
   - **Add New Project** и подключите ваш форк

### Вариант 3: Изменить Git репозиторий в существующем проекте

1. В Vercel Dashboard → ваш проект
2. **Settings** → **Git**
3. Нажмите **Disconnect** на старом репозитории
4. Нажмите **Connect Git Repository**
5. Выберите ваш форк (`amiroesh1/myfolio`)

## Важные переменные окружения для Vercel:

Добавьте в **Settings** → **Environment Variables**:

```
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard
DATABASE_URL=postgresql://...
NEXT_PUBLIC_SUPABASE_URL=https://...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
ADMIN_USER_IDS=user_...
```

## Проверка:

После подключения:
1. Vercel автоматически запустит деплой при каждом push в `main`
2. Проверьте **Deployments** вкладку - должны появиться новые деплои
3. URL вашего проекта должен обновиться

## Если всё ещё не работает:

1. Проверьте что вы пушите в правильный репозиторий:
   ```bash
   git remote -v
   ```
   Должно быть: `https://github.com/amiroesh1/myfolio.git`

2. Убедитесь что вы на ветке `main`:
   ```bash
   git branch
   ```

3. Проверьте что коммиты запушены:
   ```bash
   git log origin/main -5
   ```

4. В Vercel Dashboard проверьте:
   - **Settings** → **Git** → Production Branch должен быть `main`
   - **Deployments** → должны быть новые деплои после ваших коммитов

